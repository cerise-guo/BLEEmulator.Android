package com.blesim.bluetooth;

import android.app.Activity;
import android.content.Context;

/**
 * Created by cerise on 4/21/18.
 */

/*
public class BleSimActivity extends Activity {
    final private String TAG = BleSimActivity.class.getSimpleName();

    @Override
    public Object getSystemService(String name) {
        GLog.d( name);

        if (Context.BLUETOOTH_SERVICE.equals(name)) {
            return new BluetoothManager().sharedInstance();
        }
        return super.getSystemService(name);
    }
}
*/